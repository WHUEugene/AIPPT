# ToDo（更新于 2026-03-11）

## 高优先级
- [x] 优化 `launch_aippt.command`：启动完成后自动打开 `http://localhost:5173`，优先拉起 Edge / Chrome，失败时回退系统默认浏览器，避免手动复制地址。
- [x] 修复模板列表中“缺少示例图时自动补图”流程：当前打开页面后未成功生成，出现无图报错与难看的空白状态；需要保证补图成功，或至少做稳定的失败兜底与占位展示。
- [x] 优化 workshop 左侧插页入口样式：当前“在此插入一页”按钮过宽、过抢视觉，先收窄宽度并去掉文字，仅保留更轻量的加号触发。
- [x] 为 workshop 增加“删除本页”入口，并处理删除后的选中页切换、页码重排，以及最后一页/最少保留页数的交互约束。
- [x] 重做 workshop 重绘状态提示：当前只重绘 1 张图时显示“正在重绘 1/3 张图片”，分母实际是并发上限而不是任务总数，容易误导；同时状态提示不要压在 PPT 视觉区域里，改到顶部用更轻量的小字/状态条展示。
- [ ] 用新的模板分析逻辑重刷现有模板库里质量一般的旧 `style_prompt`，至少检查最后一个“未命名模版”。
- [ ] 为模板示例图生成补一个专用后端接口，避免当前前端直接复用普通单页生图接口。
- [ ] 为 PPTX 导出补回归测试，覆盖无图、异常图路径、不同图片比例导出。

## 中优先级
- [ ] 为 LLM 网关兼容层补自动化测试，覆盖 OpenRouter 与 OpenAI 兼容网关的路径回退、响应解析、错误处理。
- [ ] 梳理模板图片输出目录与静态资源目录的关系，收敛 `generated/images` 与历史目录的使用方式。
- [ ] 清理本地配置与示例配置中的敏感信息管理策略，避免误提交 API Key。

## 低优先级
- [ ] 给模板选择页补“最近编辑/最近使用”排序，降低模板数量增多后的查找成本。
