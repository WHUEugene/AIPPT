# ToDo（更新于 2026-03-03）

## 已完成
- [x] 接入 ChatFire（OpenAI 兼容）并保留 OpenRouter 兼容能力。
- [x] 修复连接测试解析失败问题（HTML 非 JSON 场景）。
- [x] 增加请求重试与错误信息增强，降低上游瞬时失败影响。
- [x] 修复 PPTX 导出 `PipelineLogger` 调用不兼容导致的崩溃。
- [x] 完成本地链路验证：文本、图像、导出流程。

## 进行中
- [ ] 将指定项目绑定到目标模板并触发全量重新生成（含出图）。

## 待办
- [ ] 为 LLM 网关兼容层补充自动化测试（路径回退、响应解析、错误回退）。
- [ ] 为导出链路补充回归测试（无图/有图/异常图片路径场景）。
- [ ] 梳理并收敛本地配置中的敏感信息管理策略（避免误提交 API Key）。
