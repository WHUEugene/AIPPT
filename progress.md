# 进度记录

## 2026-03-10
- 拆分文本与图片网关路由：文本模型可走 OpenRouter，NanoBanana 图片模型可走 ChatFire。
- 关闭文本失败回退逻辑，文本生成失败时直接报错，不再返回默认占位结果。
- 修复批量生图并发落到默认 `5` 的问题，当前按图片数量并发执行，受后端上限控制。
- 修复 `Workspace` 单页重绘交互：支持最多 3 张图片并发重绘，并增加顶部状态条与缩略图重绘标记。
- 修复 PPTX 导出比例错误：整套页面比例优先跟首张有效图片走，避免导出后页面被压扁。
- 优化 PPTX 导出体积与质量：导出前统一重编码图片，保留较高质量并显著降低文件体积。
- 恢复中断批量生图后的项目数据：将已落盘图片重新挂回项目文件，恢复历史项目 `03102`。
- 补齐模板管理能力：模板选择页支持编辑，模板创建页升级为新建/编辑共用页。
- 支持无封面模板一键生成示例图，并补全当前模板库缺失的模板封面。
- 重写模板图片风格分析提示词：从“分析报告”改为“模板级生图规则”，增加留白、饱和度、对比度、色彩丰富度等客观统计。

## 2026-03-03
- 接入 OpenAI 兼容网关能力（ChatFire）：`/chat/completions` 与 `/v1/chat/completions` 自动回退，兼容不同网关路径。
- 保留 OpenRouter 兼容：仅在 OpenRouter 域名下附加 `HTTP-Referer` 与 `X-Title` 请求头。
- 增强 LLM 调用稳定性：对 `429/5xx` 增加轻量重试与更清晰的错误信息输出。
- 完成图像返回兼容解析：支持 `message.images`、`image_url`、Markdown 图片链接、`images/generations` 的 URL 与 Base64。
- 修复配置连接测试流程：前后端支持 `timeout_seconds`，前端按后端 `success` 字段判定测试结果。
- 修复 PPTX 导出崩溃：为 `PipelineLogger` 增加标准日志代理方法，解决导出时 `AttributeError`。
